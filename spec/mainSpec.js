describe("Palindrome", function() {

  it("should take a string as a parameter", function() {
    expect(str).toBeString();
  });

  it("should return true or false", function() {
    expect(isPalindrome(str)).toBe(true || false);
  });
});
